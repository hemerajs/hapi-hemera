'use strict';

// Load modules

const Code = require('code');
const Hapi = require('hapi');

describe('Basic', function () {

    before(function (done) {

        done()
    })

    after(function (done) {

        done()
    })

    it('Test something here', function (done) {

        done()
    })
})